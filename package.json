{
  "name": "tyleryep.github.io",
  "version": "0.1.0",
  "description": "tyleryep.com",
  "homepage": "https://tyleryep.com",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-bootstrap": "^2.0.0",
    "react-dom": "^17.0.2",
    "react-scripts": "^4.0.3"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "pre-push": "^0.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b main -d build",
    "lint": "npx eslint public src"
  },
  "pre-push": "deploy",
  "eslintConfig": {
    "extends": "react-app",
    "ignorePatterns": "zenscroll-min.js"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
